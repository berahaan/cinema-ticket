import { gql } from '@apollo/client/core';
export const VALIDATE_TOKEN = gql`
        mutation ($token: String!) {
          authenthicate(token: $token) {
            isvalid
          }
        }
      `;