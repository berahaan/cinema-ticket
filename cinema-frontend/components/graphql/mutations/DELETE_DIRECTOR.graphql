import { gql } from '@apollo/client/core';
export const DELETE_DIRECTOR = gql`
  mutation DeleteDirector($director_id: Int!) {
    delete_directors_by_pk(director_id: $director_id) {
      director_id
    }
  }
`;