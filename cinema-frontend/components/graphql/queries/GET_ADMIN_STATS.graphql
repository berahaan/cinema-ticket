import { gql } from '@apollo/client/core';

export const GET_ADMIN_STATS = gql`
  query GetAdminStats {
    total_users {
      total_count
    }
    total_movies {
      total_count
    }
    total_schedules {
      total_count
    }
  }
`;